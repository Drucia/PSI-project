import { EducationProgram } from './models/education-program';

describe('EducationProgram', () => {
  it('should create an instance', () => {
    expect(new EducationProgram()).toBeTruthy();
  });
});
