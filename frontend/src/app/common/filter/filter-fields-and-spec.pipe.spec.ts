import { FilterFieldsAndSpecPipe } from './filter-fields-and-spec.pipe';

describe('FilterFieldsAndSpecPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFieldsAndSpecPipe();
    expect(pipe).toBeTruthy();
  });
});
